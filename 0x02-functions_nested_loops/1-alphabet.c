#include <stdio.h>
#include "main.h"

/**
 * main - the functin of the code
 *
 * Return: (0) is success
 */
int main(void)
{
	char b;

	for (b = 'a'; b <= 'z'; b++)
		_putchar(b);

	_putchar('\n');

	return (0);
}
