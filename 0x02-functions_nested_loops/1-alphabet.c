#include "main.h"

/**
 * main - the defined function
 *
 *Return: (0) means success
 */
int main(void)
{
char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
		_putchar(alphabet);
	return (0);
}
