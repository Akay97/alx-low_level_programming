#include "main.h"

/**
 * _abs - the function name
 * @num: the value to be computed
 * Return: (0) at success
 */
int _abs(int num)
{

	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
