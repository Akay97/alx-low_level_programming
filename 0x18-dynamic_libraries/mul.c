#include <stdlib.h>
#include <stdio.h>

/**
 * mul - a function that multiply two integer
 * @a: the first integer
 * @b: the second integer
 * Return: mul
 */
int mul(int a, int b)
{
	return (a * b);
}
