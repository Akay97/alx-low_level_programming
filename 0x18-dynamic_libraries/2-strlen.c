#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - a functiion that check the lenght of a string
 * @s: the string
 * Return: the function
 */
int _strlen(char *s)
{
	return (strlen(s));
}
