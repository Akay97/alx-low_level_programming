#include <stdlib.h>
#include <stdio.h>

/**
 * sub - a function that subtrac two integer
 * @a: the first integer
 * @b: the second integer
 * Return: sub
 */
int sub(int a, int b)
{
	return (a - b);
}
