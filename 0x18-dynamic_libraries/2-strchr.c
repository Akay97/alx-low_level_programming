#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strchr - the function name
 * @s: a charater
 * @c: the second charater
 * Return: the the function
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
