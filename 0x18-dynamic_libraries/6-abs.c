#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - the function name
 * @n: an integer
 * Return: the function
 */
int _abs(int n)
{
	return (abs(n));
}
