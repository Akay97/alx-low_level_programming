#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - a funcion tha check for a digit
 * @c: the digit to check
 * Return: the function
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
