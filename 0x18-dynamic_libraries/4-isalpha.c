#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - a functioin that check for alphabeth
 * @c: the chrater to check
 * Return: the function
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
