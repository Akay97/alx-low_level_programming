#include <stdlib.h>
#include <stdio.h>

/**
 * mod - a function that find the modulus of two integer
 * @a: the first integer
 * @b: the second integer
 * Return: mod
 */
int mod(int a, int b)
{
	return (a % b);
}
