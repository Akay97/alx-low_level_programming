#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcpy - a functiion that copy a string
 * @dest: the destination
 * @src: the source
 * Return: the function
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
