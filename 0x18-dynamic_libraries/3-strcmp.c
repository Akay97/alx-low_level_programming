#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcmp - a function that compare strings
 * @s1: the first string
 * @s2: the second string
 * Return: the function
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
