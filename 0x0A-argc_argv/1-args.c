#include <stdio.h>

/**
 * main - the function name
 * @argc: the argumanets count
 * @argv: the aru=gumanats variable
 * Return: 0 as success
 */

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc - 1);
	return (0);
}

