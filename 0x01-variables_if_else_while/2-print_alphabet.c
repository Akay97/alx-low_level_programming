#include <stdio.h>

/**
 * main - a defined function
 *
 * Return: returning (0) is success
 */
int main(void)
{
char alphab;
alphab = 'a';
	while
		(alphab <= 'z') {
			putchar(alphab);
			alphab++;
		}
	putchar('\n');
	return (0);
}
