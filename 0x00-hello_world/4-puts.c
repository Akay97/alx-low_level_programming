#include <stdio.h>

/**
 * main - is the entry point of the code. It is the start of the code
 * This code is meant the give the output Hello world.
 *
 * Return: the code will always return 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
