#include <stdio.h>

/**
 * main - is the starting of the program
 *
 * Return: when (0) is returned, it is a success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
